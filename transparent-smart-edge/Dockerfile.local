# Optional HAOS-local hotfix layer. The normal release is built by Dockerfile.
ARG BUILD_FROM=local/aarch64-addon-bezrabotnyi_transparent_smart_edge:0.1.0
FROM ${BUILD_FROM}

RUN apk add --no-cache iproute2 nftables

COPY rootfs /

RUN chmod 0755 /usr/bin/telegram-tproxy-policy.sh
